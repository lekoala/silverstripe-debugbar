# Contributing

## To setup the SCSS and Javascript, you need to take the following steps

- In the Debugbar folder, run `yarn install`
- If you want to change CSS, change it in the `assets/scss` folder
- Run `yarn package`
- If you want to change javascript, change it in the `assets/jsssrc` folder
- Run `yarn package`